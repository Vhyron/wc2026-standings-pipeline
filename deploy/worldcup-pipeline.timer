[Unit]
Description=Daily World Cup Analytics pipeline run

[Timer]
OnCalendar=*-*-* 09:00:00
# If the Pi was off at 09:00, run on next boot instead of skipping the day.
Persistent=true

[Install]
WantedBy=timers.target
